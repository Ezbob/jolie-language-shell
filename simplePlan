PLAN for MicroService project

- Create a docker image for running Jolie services (DONE)
- Create Jolie programmes according to the architecture outlined below
- Create frontend for use on the Jolie website 


Overall architecture:

 - DockerJolie Manager:
 	ROLE: A service that manages creating and registrating new docker embedded jolie programmes.
 	Serves as a admin for the clients, controlling the security parameters for the docker sandbox

 - Client:
 	ROLE: Primary User of the docker sandbox. 
 	Requests the docker sandboxes from the the manager.
 	Sends code to the sandbox for evaluation.

 - Sandbox:
 	ROLE: Code evaluator contained in a Docker container. 
 	Accepts code from the client and reponds with the results (can timeout)